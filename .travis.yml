#----------------------------------------------------------------
# Travis-CI configuration for R packages
#
# REFERENCES:
# * Travis CI: https://docs.travis-ci.com/user/languages/r
# * covr: https://github.com/jimhester/covr
#
# YAML validated using http://www.yamllint.com/
#----------------------------------------------------------------
language: r
sudo: false
cache: packages
warnings_are_errors: false
r_check_args: --as-cran
latex: false

r_packages:
  - future.BatchJobs
  - future.batchtools

bioc_packages:
  - Biobase

matrix:
  include:
    - os: linux
      r: oldrel
    - os: linux
      r: release
    - os: linux
      r: release
      env:
        - R_FUTURE_FORK_ENABLE=false
    - os: linux
      r: release
      env:
        - R_FUTURE_PLAN=multisession
    - os: linux
      r: devel
      r_packages:
        - doRNG
        - future.BatchJobs
        - future.batchtools
        - plyr
    - os: linux
      r: devel
      env:
        - R_KEEP_PKG_SOURCE=yes
        - _R_S3_METHOD_LOOKUP_BASEENV_AFTER_GLOBALENV_=true
        - _R_S3_METHOD_LOOKUP_USE_TOPENV_AS_DEFENV_=true
        - _R_CHECK_CONNECTIONS_LEFT_OPEN_=true
        - _R_CHECK_LENGTH_1_CONDITION_=true
        - _R_CHECK_LENGTH_1_LOGIC2_=true
        - _R_CLASS_MATRIX_ARRAY_=true
    - os: osx
      r: oldrel
    - os: osx
      r: release
    - os: linux
      r: release
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      r_packages:
        - covr
      after_success:
        - Rscript -e 'covr::codecov(quiet=FALSE)'
      env: NB='w/ covr' ## Just a label
    - os: linux
      r: release
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ foreach' ## Just a label
        - _R_CHECK_TESTSETS_=foreach
        - _R_CHECK_FUTURE_STRATEGIES_=sequential,multicore,multisession,batchjobs_local,batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,foreach.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - NMF
        - cluster
        - lattice
        - MASS
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ NMF' ## Just a label
        - _R_CHECK_TESTSETS_=NMF
        - _R_CHECK_FUTURE_STRATEGIES_=sequential,multicore,multisession,batchjobs_local,batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,NMF.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - TSP
        - cluster
        - foreign
        - lattice
        - MASS
        - maps
        - sp
        - maptools
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ TSP' ## Just a label
        - _R_CHECK_TESTSETS_=TSP
        - _R_CHECK_FUTURE_STRATEGIES_=sequential,multicore,multisession,batchjobs_local,batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,TSP.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - glmnet
        - cluster
        - lattice
        - MASS
        - Matrix
        - survival
        - lars
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ glmnet' ## Just a label
        - _R_CHECK_TESTSETS_=glmnet
        - _R_CHECK_FUTURE_STRATEGIES_=sequential,multicore,multisession,batchjobs_local,batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,glmnet.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - plyr
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ plyr & sequential' ## Just a label
        - _R_CHECK_TESTSETS_=plyr
        - _R_CHECK_FUTURE_STRATEGIES_=sequential
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,plyr.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - plyr
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ plyr & multisession' ## Just a label
        - _R_CHECK_TESTSETS_=plyr
        - _R_CHECK_FUTURE_STRATEGIES_=multisession
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,plyr.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - plyr
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ plyr & future.BatchJobs' ## Just a label
        - _R_CHECK_TESTSETS_=plyr
        - _R_CHECK_FUTURE_STRATEGIES_=batchjobs_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,plyr.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - plyr
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ plyr & future.batchtools' ## Just a label
        - _R_CHECK_TESTSETS_=plyr
        - _R_CHECK_FUTURE_STRATEGIES_=batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,plyr.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - plyr
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ plyr & future.callr' ## Just a label
        - _R_CHECK_TESTSETS_=plyr
        - _R_CHECK_FUTURE_STRATEGIES_="future.callr::callr"
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e "source('https://install-github.me/HenrikBengtsson/future.callr')"
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,plyr.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - caret
        - class
        - foreign
        - lattice
        - MASS
        - Matrix
        - nlme
        - nnet
        - rpart
        - survival
        - BradleyTerry2
        - e1071
        - earth
        - fastICA
        - gam
        - ipred
        - kernlab
        - klaR
        - ellipse
        - mda
        - mgcv
        - mlbench
        - MLmetrics
        - party
        - pls
        - pROC
        - proxy
        - randomForest
        - RANN
        - spls
        - subselect
        - pamr
        - superpc
        - Cubist
        - pls
        - recipes
        - mboost
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ caret & sequential' ## Just a label
        - _R_CHECK_TESTSETS_=caret
        - _R_CHECK_FUTURE_STRATEGIES_=sequential
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,caret.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - caret
        - class
        - foreign
        - lattice
        - MASS
        - Matrix
        - nlme
        - nnet
        - rpart
        - survival
        - BradleyTerry2
        - e1071
        - earth
        - fastICA
        - gam
        - ipred
        - kernlab
        - klaR
        - ellipse
        - mda
        - mgcv
        - mlbench
        - MLmetrics
        - party
        - pls
        - pROC
        - proxy
        - randomForest
        - RANN
        - spls
        - subselect
        - pamr
        - superpc
        - Cubist
        - pls
        - recipes
        - mboost
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ caret & multisession' ## Just a label
        - _R_CHECK_TESTSETS_=caret
        - _R_CHECK_FUTURE_STRATEGIES_=multisession
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,caret.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - caret
        - class
        - foreign
        - lattice
        - MASS
        - Matrix
        - nlme
        - nnet
        - rpart
        - survival
        - BradleyTerry2
        - e1071
        - earth
        - fastICA
        - gam
        - ipred
        - kernlab
        - klaR
        - ellipse
        - mda
        - mgcv
        - mlbench
        - MLmetrics
        - party
        - pls
        - pROC
        - proxy
        - randomForest
        - RANN
        - spls
        - subselect
        - pamr
        - superpc
        - Cubist
        - pls
        - recipes
        - mboost
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ caret & BatchJobs' ## Just a label
        - _R_CHECK_TESTSETS_=caret
        - _R_CHECK_FUTURE_STRATEGIES_=batchjobs_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,caret.R", chdir = TRUE, echo = TRUE)'
    - os: linux
      r: release
      r_packages:
        - future.BatchJobs
        - future.batchtools
        - caret
        - class
        - foreign
        - lattice
        - MASS
        - Matrix
        - nlme
        - nnet
        - rpart
        - survival
        - BradleyTerry2
        - e1071
        - earth
        - fastICA
        - gam
        - ipred
        - kernlab
        - klaR
        - ellipse
        - mda
        - mgcv
        - mlbench
        - MLmetrics
        - party
        - pls
        - pROC
        - proxy
        - randomForest
        - RANN
        - spls
        - subselect
        - pamr
        - superpc
        - Cubist
        - pls
        - recipes
        - mboost
      r_check_args: --no-build-vignettes --no-codoc --no-examples --no-tests --no-manual --ignore-vignettes
      env:
        - NB='w/ caret & batchtools' ## Just a label
        - _R_CHECK_TESTSETS_=caret
        - _R_CHECK_FUTURE_STRATEGIES_=batchtools_local
      script:
        - R CMD build .
        - R CMD INSTALL --no-docs doFuture_*.tar.gz
        - Rscript -e 'if (!grepl("[ci cache]", Sys.getenv("TRAVIS_COMMIT_MESSAGE"), fixed = TRUE)) source("tests/zzz_opt-in,caret.R", chdir = TRUE, echo = TRUE)'

before_install:
  - Rscript -e 'c(physical = parallel::detectCores(logical = FALSE), logical = parallel::detectCores())'
  - export _R_CHECK_TIMINGS_=0
  - _R_CHECK_LENGTH_1_LOGIC2_=false Rscript -e 'install.packages(c("lambda.r", "futile.logger"))'

notifications:
  email:
    on_success: change
    on_failure: change
